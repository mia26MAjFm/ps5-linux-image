[main]
# Devuan has no systemd-resolved. resolvconf manages /etc/resolv.conf
# directly; see the post-packages action in image.yaml for the matching
# rc-manager line. Leaving dns=default lets NM hand entries to resolvconf.
dns=default
